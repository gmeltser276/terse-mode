# macOS
.DS_Store

# Node
node_modules/
npm-debug.log*

# Runtime artifacts (these live in ~/.claude at runtime, never in-repo —
# listed defensively in case a test seeds them here)
.terse-active
.terse-history.jsonl
.terse-statusline-suffix
